# Sync roadmap

Future work may add per-app records and configurable shared-budget groups. Preserve installation identities and deduplicated usage records now so those paths remain possible. This milestone deliberately excludes server, API, companion app, account, synchronization, and public-release work.
